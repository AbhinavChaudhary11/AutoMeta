# Gemini API Key (required for workflow generation)
# Get your free API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# MongoDB Connection String (optional - uses local MongoDB if not provided)
# For MongoDB Atlas free tier, use: mongodb+srv://username:password@cluster.mongodb.net/autometa
MONGODB_URI=

# Server Port (optional - defaults to 5000)
PORT=5000

